"use client";

/**
 * Native (Capacitor) loads the same Next.js app in a WebView; sign-in uses Clerk on `/login`.
 * Legacy system-browser Google OAuth + `/api/auth/mobile/*` handoff has been removed.
 */
export function CapacitorAuthBridge() {
  return null;
}
